/**
 * 가상 링크 계층 패키지.
 * <p>
 * 단일 JVM 시뮬레이션에서 EthernetFrame을 endpoint 사이에 전달하기 위한 구조를 정의<br>
 * VirtualNic, VirtualLink, FrameReceiver를 포함하며 ARP, Switching, Routing 판단은 담당하지 않는다.
 * </p>
 */
package link;